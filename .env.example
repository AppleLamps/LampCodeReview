# API keys are entered manually through the web UI each session.
# They are never read from environment variables or disk.
# 
# If you are upgrading from a version that used .env files,
# simply paste your key into the UI on first run.
